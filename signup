<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up - Disaster Management System</title>
    <!-- Link to the same CSS file -->
    <link rel="stylesheet" href="style.css">
    <!-- Re-using the same form styles from login example -->
    <style>
        .form-container {
            max-width: 400px;
            margin: auto;
        }
        .form-group {
            margin-bottom: 15px;
            text-align: left;
        }
        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            color: #333;
        }
        .form-group input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box; 
        }
        .form-links {
            margin-top: 20px;
            font-size: 0.9em;
        }
        .form-links a {
            color: #3498db;
            text-decoration: none;
        }
        .form-links a:hover {
            text-decoration: underline;
        }
         .form-container .btn {
            width: 100%;
            padding: 12px;
        }
    </style>
</head>
<body>

    <div class="container form-container">
        <h2>Create Account</h2>
        <p>Fill in the details below to register.</p>

        <!-- 
            NOTE: The 'action' attribute should point to your server-side script 
                  that handles the registration process. 
        -->
        <form action="/handle_signup" method="POST"> 
             <div class="form-group">
                <label for="fullname">Full Name:</label> <!-- Added Full Name -->
                <input type="text" id="fullname" name="fullname" required> 
            </div>
            <div class="form-group">
                <label for="email">Email Address:</label>
                <input type="email" id="email" name="email" required> 
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required>
            </div>
             <div class="form-group">
                <label for="confirm_password">Confirm Password:</label>
                <input type="password" id="confirm_password" name="confirm_password" required>
            </div>
            <div class="form-group">
                <!-- Using the green signup button style -->
                <button type="submit" class="btn signup-btn">Sign Up</button> 
            </div>
        </form>

         <div class="form-links">
            <p>Already have an account? <a href="login.html">Login</a></p>
            <p><a href="index.html">Back to Home</a></p>
        </div>
    </div>

</body>
</html>